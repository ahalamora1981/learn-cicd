[project]
name = "learn-cicd"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi>=0.139.0",
    "httpx>=0.28.1",
    "pytest>=9.1.1",
    "uvicorn>=0.50.2",
]

[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
